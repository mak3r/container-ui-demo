apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: display-discover-daemonset
spec:
  selector:
    matchLabels:
      name: display-discover
  template:
    metadata:
      labels:
        name: display-discover
    spec:
      containers:
      - env:
        - name: DISCOVERY_HANDLERS_DIRECTORY
          value: /var/lib/akri
        image: atgracey/display-discover:latest
        imagePullPolicy: IfNotPresent
        name: display-discover
        securityContext:
          privileged: true
        volumeMounts:
        - mountPath: /var/lib/akri
          name: discovery-handlers
      volumes:
      - hostPath:
          path: /var/lib/akri
        name: discovery-handlers
---
apiVersion: akri.sh/v0
kind: Configuration
metadata:
  name: udev-display
spec:
  brokerProperties: {}
  brokerSpec:
    brokerPodSpec:
      containers:
        - name: x11
          image: registry.opensuse.org/home/atgracey/wallboardos/15.4/x11test:icewm
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: true
            privileged: true
            readOnlyRootFilesystem: false
          volumeMounts:
            - mountPath: /tmp
              name: tmp
            - mountPath: /root
              name: home
      dnsPolicy: ClusterFirst
      terminationGracePeriodSeconds: 5
      volumes:
        - name: tmp
          emptyDir: {}
        - name: home
          emptyDir: {}
  capacity: 1
  discoveryHandler:
    name: display
